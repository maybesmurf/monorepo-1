version: "3"

services:
  rest_api:
    container_name: rest_api
    build:
      context: ./api
      dockerfile: Dockerfile
    env_file:
      - .env

  web:
    container_name: web
    build:
      context: ./web
      dockerfile: Dockerfile.prod
    env_file:
      - .env
