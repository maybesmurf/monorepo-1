import { getLayout } from "@Components/layouts/Dashboard"

const Settings = () => {
	return <div>Make something great with the Settings component here!</div>
}

Settings.getLayout = getLayout

export default Settings
